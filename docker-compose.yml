services:
  postgres:
    image: postgres:16
    container_name: raxa-db
    environment:
      POSTGRES_DB: raxa
      POSTGRES_USER: raxa_user
      POSTGRES_PASSWORD: raxa_pass
    ports:
      - "5432:5432"
    volumes:
      - raxa_data:/var/lib/postgresql/data

volumes:
  raxa_data: